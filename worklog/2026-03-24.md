# 2026-03-24 Guandan Scoreboard Worklog

## UI & Aesthetics Transformation
- **Dual-Team Score Tracking**: Refactored the core mechanism into a strict 2-team dynamic with comprehensive history stack integration (allowing exact level-undos).
- **Responsive Layout**: Overhauled interface to a pure `100dvh` flex-box rendering. Replaced screen-width queries simply with `landscape:` attributes to organically flip dual vertical stacks into horizontal side-by-side orientations when arbitrarily rotating the device footprint.
- **Custom Theming System**: Built a modular, dynamically looping 8-color Palette library exclusively enforcing no-duplicate allocations across concurrent teams.
- **Hex Variations & Legibility**: Standardized backgrounds on robust `%50` translucent base tints layered beneath deeply frosted `bg-white/60` scorecards—guaranteeing heavy color saturation matched synchronously with pure font contrast readability. 

## Logic Systems
- **Score Caps (`A1`)**: Re-coded the bounds so standard `+2` and `+3` incrementation commands automatically clamp and halt cleanly exactly against `A1` bounds.
- **Dealer (Crown) Tracking**: Developed an interactable Crown icon tied to `leadingTeam` status for determining the starting deck authority manually throughout progressive rounds.
- **Explode (A3) Condition**: Reaching absolute limit transforms the increment array sequentially into a warning pulse bomb. Selecting it opens an Exploding reset menu providing teams absolute autonomy over whether explicitly retaining or passing Dealer (庄家) authorities to opponents when forfeiting back to level 2.
- **Victory (Trophy) Mechanic**: Successfully arriving between terminal levels `A1 - A3` introduces a golden Celebration pop-over dialogue, enabling comprehensive scoreboard victory resets.

## Housekeeping Validations
- Excluded unrequired pre-bundled templates and routing arrays, shifting standard operations purely to a root-defined Single Page orientation.
- Overhauled Tab metadata alongside deploying native SVG Emojis explicitly preventing iOS-centric zoom-scaling malfunctions.
